"""Ports — interfaces owned by the application core (Blueprint §3.3).

LLMPort, EmbeddingPort, VectorSearchPort, QueuePort, StoragePort,
RepositoryPorts, ClockPort land with their first consumers (S1+).
"""
