# aether-web

React 18 + TypeScript + Vite SPA (Blueprint Ch. 5, ADR-5.1..5.5).

**Sprint 0 state:** placeholder shell only — proves build/lint/type
pipelines and the Docker image. Routing, state architecture (TanStack
Query + Zustand), and the fetch-stream SSE client land from Sprint 3.
