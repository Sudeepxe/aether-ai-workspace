/**
 * Sprint 0 placeholder shell. The real SPA (routing, auth, streaming chat)
 * lands from Sprint 3 per the Blueprint Ch. 5 design. This page exists so
 * the build pipeline, lint, and Docker image have a real artifact to prove.
 */
export function App(): JSX.Element {
  return (
    <main style={{ fontFamily: "system-ui", padding: "4rem", maxWidth: 640 }}>
      <h1>Aether AI Workspace</h1>
      <p>
        Repository scaffold (Sprint 0). No product surface yet — see the roadmap milestones S0–S12
        in the repository.
      </p>
    </main>
  );
}
