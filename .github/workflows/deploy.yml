# Deploy pipeline stub (D10-2). Sprint 0: build/sign/push shape only,
# manual trigger. SSH rolling deploy + auto-rollback land in S11.
name: deploy
on: [workflow_dispatch]
jobs:
  build-sign-push:
    runs-on: ubuntu-latest
    permissions: { contents: read, packages: write, id-token: write }
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - run: echo "S11 — build, SBOM (syft), cosign sign, push to GHCR by digest, SSH deploy"
