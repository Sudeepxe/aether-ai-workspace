# SOPS configuration (ADR-7.5). Recipient is the repo owner's own age
# public key (generated via `age-keygen`, private key never committed —
# see infra/secrets/README.md for the bootstrap process). Superseded the
# Sprint 0 demo key that proved the mechanism round-trips.
creation_rules:
  - path_regex: infra/secrets/.*\.enc\.yaml$
    age: "age1ngslnaavftjgan67etu0jpekqyntp22a9t59p3vytqmfek8f737sw4eghu"
