# gitleaks ignore list — every entry here is a scoped, reviewed exception
# for a value gitleaks can't distinguish from a real leak on its own (same
# "known-gaps register, not a confession" posture as .trivyignore).
#
# The AETHER_JWT_SIGNING_KEY default in .env.example (and the identical
# literal in apps/api/src/aether/config.py's Settings.jwt_signing_key) is a
# dev-only Ed25519 seed, generated for this scaffold, committed
# intentionally — the same posture Sprint 0 already established for
# database_url/database_migrator_url's dev-only passwords (see config.py's
# module docstring). It signs nothing outside a local `dev` compose
# profile; production overrides it via the SOPS bundle (ADR-7.5), never
# from a committed .env. It necessarily reads as high-entropy secret
# material to gitleaks' generic-api-key rule because it IS real key
# material — that's unavoidable for a value that has to actually decode as
# a 32-byte Ed25519 seed, unlike the low-entropy dev passwords that don't
# trip the same heuristic.
305b85553b37ea831c3f499139cf1ef4b44a9577:.env.example:generic-api-key:24
